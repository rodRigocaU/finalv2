#include "Menu.h"
#include <SFML/Graphics.hpp>
#include <iostream>
using namespace std;


void Menu::startMenu(sf::RenderWindow(&menudisplay))
{


       /* sf::Texture Menu;
        Menu.loadFromFile("Imagenes/menu.jng");
        Menu.setSmooth(true);
        sf::Sprite Menu_s;
        Menu_s.setTexture(Menu);


        while(menudisplay.isOpen())
        {

                menudisplay.setFramerateLimit(FPS);
                menudisplay.clear(Menu_s);
                if(sf::Keyboard::isKeyPressed(sf::Keyboard::Escape)){menudisplay.close();}
                if(sf::Keyboard::isKeyPressed(sf::Keyboard::Escape)){menudisplay.close();}

                menudisplay.draw(Fruit);
                menudisplay.display();


            sf::Event e;
            while(menudisplay.pollEvent(e))
            {
                if(e.type == sf::Event::Closed)
                    menudisplay.close();
            }
        }*/
}
